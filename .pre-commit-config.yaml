repos:
  - repo: local
    hooks:
      - id: repo-lint
        name: repo-lint (tools/lint.py)
        entry: python -X utf8 tools/lint.py
        language: system
        pass_filenames: false
        stages: [pre-commit, pre-push]
