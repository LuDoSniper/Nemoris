repos:
  - repo: local
    hooks:
      - id: repo-lint
        name: repo-lint (tools/lint.py)
        entry: python tools/lint.py
        language: system
        pass_filenames: false
        stages: [commit, push]
